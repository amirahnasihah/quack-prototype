# PlatformIO — build output & downloaded libs (rebuilt via `pio run`)
.pio/

# Secrets / local config (never commit WiFi keys, API keys)
.env
.env.*
secrets.h
sdkconfig.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE (optional local settings — keep extensions.json in repo)
.vscode/launch.json
.vscode/c_cpp_properties.json
.cursor/

# Logs & temp
*.log
tmp/
