# PlatformIO — build output & downloaded libs (rebuilt via `pio run`)
.pio/

# Python — voice-agent virtualenv & caches (rebuilt via `pip install -r requirements.txt`)
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# Secrets / local config (never commit WiFi keys, API keys)
.env
.env.*
secrets.h
sdkconfig.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE (optional local settings — keep extensions.json in repo)
.vscode/launch.json
.vscode/c_cpp_properties.json
.cursor/

# Logs & temp
*.log
tmp/

# Wokwi CLI (local download for chip compile)
.tools/
chips/wokwi-api.h

